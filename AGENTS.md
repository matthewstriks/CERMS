# Firebase protection — explicit user requirement

## Database boundary

The user authorized the initial security rules for `cerms`. The tested baseline in `firebase/cerms.rules` was deployed only to `cerms`; `(default)` rules were verified unchanged. Preserve own-profile privacy, club-scoped read-only business access, the designated UID-only access switch, and default denial for undefined collections/subcollections. Future rules changes require authorization for their scope. Run `npm run test:rules` for synthetic local policy tests; never probe production writes. See `docs/CERMS-SECURITY-RULES.md`.

The user explicitly authorized trimming every `cerms/users` document to `uid`, `displayName`, `email`, `access`, `rank`, and `version`, removing permissions and all other profile fields. Use the document ID for `uid` and preserve existing values of the other retained fields. The user explicitly requested leaving missing versions absent; do not populate them automatically. Preserve other missing profile values rather than inventing them. This maintenance authorization applies only to `cerms`, does not permit changes to `(default)` or Firebase Auth, and does not enable general desktop profile writes.

The user explicitly authorized a one-time import of all legacy Firestore `users` profiles from `(default)` into `cerms`, preserving document IDs and profile fields so existing project-level Auth accounts can use CERMS 5.0. Source access remains read-only. This does not authorize copying other collections, changing Auth accounts, deploying rules, or enabling general desktop profile writes.

The `(default)` Firestore database in project `cerms-7af24` is the current live legacy CERMS database. Do not change its data, rules, indexes, configuration, backups, or services. CERMS 5.0 targets the separate named database `cerms` in that same project. Keep both desktop Firestore clients and the request guard restricted to `cerms`; never fall back to `(default)`. The system-switch write exception below applies only to the CERMS 5.0 target `cerms`, never to `(default)`.

The user intends to build the new version on `cerms`. This database selection does not by itself authorize migrations, copying legacy records, deployments, or additional live writes. The specific permissions and write protections below still apply. Authentication and Storage remain project-level/shared resources and must not be treated as isolated by selecting a named Firestore database.

Never create, update, delete, migrate, seed, import, deploy, or otherwise change the existing Firebase database, documents, indexes, rules, configuration, Storage objects, or backend services without the user's explicit permission for that action. Permission to connect, inspect, or develop a screen is not permission to write data.

The current Membership integration must remain read-only. Do not add working create/edit/delete/check-in/renewal actions or enable database writes as part of unrelated work. Do not use Admin SDK credentials in the desktop. Never change access profiles or user-version fields on sign-in. Preserve the read-only network protection and its tests.

Use synthetic records for automated tests. Never seed production or test write protection by attempting real production mutations. Existing Firebase email/password sign-in is allowed for the requested connection; do not create accounts, change passwords, or deploy authentication changes without permission.

The user explicitly requested Forgot password on the opening login screen. User-triggered Firebase password-reset emails are authorized for that flow only. Keep direct account/password changes blocked; mock reset email requests in automated tests, and never send real emails as a test.

The user explicitly authorized a system switcher restricted to Firebase Auth UID `c7D7AH07kgXmjn8tSiOgzHscLZ12`. A deliberate selection may update ONLY `users/c7D7AH07kgXmjn8tSiOgzHscLZ12.access` to an existing `system` document ID. This is the sole Firestore-write exception. Keep it separate from the read-only membership reader. Never write access on sign-in, change another user, modify system documents, add profile fields, or loosen other write protections. Do not perform live switches for testing; all automated switch tests must use synthetic intercepted responses. Backend rules/configuration changes still require separate permission.

Admissions & rentals is authorized for live READ-ONLY Firestore subscriptions. Preserve the legacy `activity` query (`active == true`, `access == current system`) and hide `goingInactive` visits. Never port the legacy check-in/out, rental, renewal, in/out, waitlist, or automatic profile mutations as part of this integration. The Listen stream is allowed only for the existing database; the Write stream remains blocked. Tests may change synthetic records in an isolated local `demo-` emulator project, never in production.

The user authorized auditing and building member creation ONLY in an isolated test environment, with all legacy form options except waiver handling. `npm run dev:members` and `npm run test:member-creation` use a disposable loopback emulator fixed to `demo-cerms-members`; synthetic writes there are permitted. Keep the local creator out of the desktop entry point/build, keep live creation disabled, and preserve production request guards. Do not interpret this authorization as permission to deploy, migrate, seed production, or enable production member writes. See docs/MEMBER-CREATION.md for the compatibility boundary and unresolved cross-client allocation/payment requirements.
